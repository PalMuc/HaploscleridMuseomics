# Read in the tree trace and construct the consensus tree tree #
trace = readTreeTrace("output_bayesian_tree_posterior.trees", treetype="non-clock")
trace.setBurnin(0.25)

# Summarize tree trace and the consensus tree to file
map_tree = mapTree(trace, file="CladeC_30p_bayesian.map.tre")
con_tree = consensusTree(trace, file="CladeC_30p_bayesian.majrule.tre")
